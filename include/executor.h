#ifndef EXECUTOR_H
#define EXECUTOR_H

// dùng để thực thi các hàm không phải trong builtins
// tức là dựa vào path vars để xem trong folder /bin có lệnh đó không

void execute_cmd();
void execute_bat_file();

#endif // EXECUTOR_H
